# Nested git repos — these have their own remotes and should not be tracked by the parent
docs/growthx/

# OS
.DS_Store

# Editor
.cursor/
.vscode/

# Secrets — never commit API keys, tokens, or local env overrides
.env
.env.*
!.env.example
*.local
secrets/

# Python
.venv/
__pycache__/
*.pyc
