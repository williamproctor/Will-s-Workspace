# Workspace secrets — copy to .env and fill in real values.
# .env is gitignored; never commit real keys.

# OpenAI — used for gpt-image-2 thumbnail / illustration generation
# (see AV-AI-Newsletter/scripts/generate_thumbnail.py)
OPENAI_API_KEY=sk-...
